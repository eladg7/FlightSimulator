<Window x:Class="FlightSimulatorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlightSimulatorApp"
        mc:Ignorable="d"
        Title="Flight Simulator App" Height="450" Width="800">
    <Grid x:Name="mainGrid" Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="SeconderyGrid" Grid.Column="1" Margin="5"  >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="simDashBoard"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.CanContentScroll="True"
              HorizontalAlignment="Stretch" Text="{Binding VM_dashboard}" IsReadOnly="True" FontSize="16" FontWeight="Bold">
            </TextBox>

            <Viewbox x:Name="JoystickViewBox" Grid.Row="1" Margin="5" Stretch="Uniform">
                <local:Joystick x:Name="Joystick"/>
            </Viewbox>
        </Grid>

        <Viewbox x:Name="mapViewBox" Grid.Column="0"  Stretch="Uniform">
        <local:BingMap x:Name="myMap" />
        </Viewbox>

    </Grid>
</Window>
